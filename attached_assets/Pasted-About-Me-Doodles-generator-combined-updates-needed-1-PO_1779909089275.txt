About Me / Doodles generator — combined updates needed:

1. POST TYPE OUTPUT: add a dropdown letting user choose "Save as Post", "Save as Reel" (1080x1920), or "Save as Trial Reel" (private, uses existing trial reel flow). When Reel or Trial Reel selected, preview canvas switches to 9:16.

2. TITLE AND SUBTITLE TEXT OVERFLOW: title and subtitle currently spill off the canvas if too long. Fix: auto-wrap to multiple lines, apply max-width (80% of canvas), optionally auto-scale font size down 10-20% if wrapping still overflows. Live preview reflects in real time.

3. TEXT ALIGNMENT: add Left/Centre/Right alignment buttons under title and subtitle. Default Centre.

4. OPTIONAL TEXT BACKGROUND PANEL behind TITLE AND SUBTITLE: optional toggle to add a semi-transparent rectangle behind the title or subtitle for legibility. Controls: background colour picker (default black), transparency slider (0-100%, default 50%), corner radius (0-30px, default 12px), padding (4-32px, default 16px). Live preview.

5. Z-INDEX BUG: some text elements render BEHIND the cut-out photo. Fix layer order so EVERY text element, sticker, doodle, and logo renders ON TOP of the cut-out photo and blurred background. Order back to front: blurred background → overlay tint → cut-out photo → text elements → stickers → doodles → logo.

6. ADD GENERATE CAPTION feature: add a Generate Caption button below the design controls. Uses existing AI caption generation (OpenAI gpt-5.2) with words + title + subtitle + brand voice profile as context. Generated caption appears in an editable field for the user to tweak.

7. 3D STICKER WORD CONCEPT — KEEP AND POLISH:

The puffy 3D sticker style for individual WORD elements is correct and stays. Each word is rendered as:
- White rounded panel (border-radius around 20px)
- Bold uppercase word inside
- Chunky 2-layer outline in user's chosen accent colour
- Soft drop shadow underneath (8px y-offset, 16px blur, 30% opacity)
- Slight random rotation per sticker (-8° to +8°) for casual feel
- Topper options on top of each sticker: rainbow arch, 3D heart, 3D star, lipstick icon, mirror icon, wine glass icon (latest three from Vanessa's recent additions)
- Optional small sparkle in opposite corner

Stickers remain fully draggable, resizable, rotatable.

8. REMOVE ANY STRAY DECORATIVE HEARTS NOT TIED TO A STICKER. The only hearts in the design should be:
   - Hearts chosen as a topper on individual word stickers
   - Anywhere the USER has deliberately added one as a doodle
   
   Any auto-appearing hearts on the title, subtitle, or floating around the canvas unattached to a sticker should be removed.

Restart api-server when done.